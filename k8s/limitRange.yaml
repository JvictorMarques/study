apiVersion: v1
kind: LimitRange
metadata:
  name: resource-limits
spec:
  limits:
  - type: Container #TODO Define limits for containers
    default:
        memory: "128Mi"
        cpu: "500m"
    defaultRequest:
        memory: "64Mi"
        cpu: "250m"